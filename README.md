This is a simple workout app that runs natively on Ubuntu as a .desktop file. The project is for casual Ubuntu users that just want an simple
app, no hassle.

**Get started**:
- Clone the project
```git clone https://github.com/a6gitti/workout-tracker.git ```

- Open the folder:
  · Launch app by double-clicking the "Workout Tracker.desktop" file
